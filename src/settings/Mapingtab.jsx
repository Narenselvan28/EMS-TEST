const MappingTab = ({ mappingOptions, handleChange }) => {
    return (
        <div className="space-y-6">
            <h2 className="text-2xl font-bold text-gray-800">Mapping Options</h2>
            {/* Rest of the mapping tab content */}
        </div>
    );
};

export default MappingTab;